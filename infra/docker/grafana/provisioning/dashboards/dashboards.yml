apiVersion: 1

providers:
  - name: CoESCD
    orgId: 1
    folder: CoESCD
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
